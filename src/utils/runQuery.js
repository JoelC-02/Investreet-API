import { createTables, deleteFromTables, dropTables, insertIntoTables } from './queryFunctions';

(async () => {
  // await createTables();
  await insertIntoTables();
  // await dropTables();
  // await deleteFromTables();
})();